# Student-Attendance-Management-System
Student Attendance Management System is a Python-based desktop application used to manage attendance digitally. It allows teachers to add students, mark attendance, search records, and generate reports, while students can view their attendance details. The project uses CustomTkinter for the GUI and SQLite for data storage.
